app:
  otp:
    dev-expose: false

cors:
  allowed-origins: http://localhost:5173,https://solariotscada.vercel.app

logging:
  level:
    com.qpaix.geda: DEBUG
    org.hibernate.SQL: DEBUG
    org.hibernate.orm.jdbc.bind: TRACE
